#include <iostream>
int main()
{
   int m = 1, n = 2, p =3; 
   int min =(m < n ? (m < p ? m : p) : (n < p ? n : p)); 
   cout << min << endl
   
   
   int a = 1, b = 2;
   int i = a<b?a:b
   cout << i;
   return 0;
}
