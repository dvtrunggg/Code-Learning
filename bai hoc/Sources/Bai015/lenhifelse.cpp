#include <iostream.h> 
int main() 
{ 
	int a,b; 
	cout << "Nhap a: ";  
	cin >> a;
    cout << "Nhap b: ";  
	cin >> b;
	if(a>b)
		cout << a << " la so lon nhat.";
	else
		cout << b << " la so lon nhat.";
	return 0; 
} 

